import DashboardPage from './DashboardPage';

// For now, the Cars page renders the same dashboard content.
// The dashboard already shows the full vehicle management UI.
function CarsPage() {
  return <DashboardPage />;
}

export default CarsPage;
